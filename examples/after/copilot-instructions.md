# Acme Platform — Copilot Instructions

- Code only, no explanation unless asked.
- No secrets in code; read credentials from env vars / the secret manager.

(Language-, test-, and infra-specific rules live in `.github/instructions/*.instructions.md`,
scoped with `applyTo`.)
